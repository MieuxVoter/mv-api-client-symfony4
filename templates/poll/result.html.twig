{% extends 'base.html.twig' %}

{% block title %}{{ poll.subject }}{% endblock %}

{% block main_container %}

    <h2>{{ poll.subject }}</h2>
{#    <p>#}
{#        {% for grade in poll.grades %}#}
{#            {{ grade.name }}{{ not loop.last ? ',' : ' ?' }}#}
{#        {% endfor %}#}
{#    </p>#}
    <ul>
        {% for proposal in poll.proposals %}
            <li>
                {{ proposal.title }}

            </li>
        {% endfor %}
    </ul>

    <ul>
        {% for proposalResult in result.leaderboard %}
            <li>
                {{ proposalResult.proposal.rank }}
                {{ proposalResult.proposal.title }}
            </li>
        {% endfor %}
    </ul>

</div>
{% endblock %}
